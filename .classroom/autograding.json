{
  "tests": [
    {
      "name": "Setup & Build (no points)",
      "run": "sudo apt-get update && sudo apt-get install -y build-essential qemu-system-x86 gcc make python3-pip && pip3 install pexpect && make -j$(nproc)",
      "timeout": 1200,
      "points": 0
    },
    {
      "name": "Run grader",
      "run": "python3 ./.github/grade_proj1.py --timeout 120",
      "timeout": 1800,
      "points": 100
    }
  ]
}
